letUn copyList(x:LinList) = 
    if-null x
    then null
    else split x as carx cdrx
         copy carx as xx1 xx2
         letLin yy = copyList(cdrx)
         split yy as yy1 yy2
         letLin rt1 = linCons(xx1,yy1)
         letLin rt2 = linCons(xx2,yy2)
         linCons(rt1,rt2)



letUn freeList = fun(x:LinList){
    if-null x
    then freeList(x)
    else split x as x1 x2
         begin 
            freeAtom(x);
            freeList(x2)
         end
}

letUn x = 10
letUn y = 20

letLin res1 = newRes("aaa")
letLin res2 = newRes("bbb")

letLin res2 = liCons(res1, res2)

split copyList(res2) as a1 a2li
split a2li as a2 anil

begin
    letLin t1 = printList(a1);
    letLin t2 = printList(a2);
    freeList(t1);
    freeList(t2);
    freeList(anil)
end



